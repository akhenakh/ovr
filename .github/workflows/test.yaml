name: Tests

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
          go-version: [ '1.26' ]

    steps:
      - uses: actions/checkout@v4
      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: ${{ matrix.go-version }}
      - name: Test with Go
        run: go test -v ./...
      - name: Test with Go (geo)
        run: go test -v -tags geo ./...
